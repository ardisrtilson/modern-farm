import {createPlan} from "./plan.js"

const plan = createPlan()

export const plantSeeds = (plantingPlan) => {
    for (step of plantingPlan) {
        
    }
}

for (const seeds of plantingPlan) {

    }
}

console.log(plantingPlan)